return {
  basic = require("terminal.utils.basic"),
  buffer = require("terminal.utils.buffer"),
  lazy = require("terminal.utils.lazy"),
  win = require("terminal.utils.win"),
  fs = require("terminal.utils.fs"),
  ui = require("terminal.utils.ui"),
}
